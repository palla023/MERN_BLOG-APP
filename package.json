{
  "name": "blog_app",
  "version": "1.0.0",
  "description": "Blog Application",
  "main": "server.js",
  "type": "module",
 "scripts": {
    "server": "node server/server.js",
    "dev": "nodemon server/server.js",
    "build": "npm install && npm install  --legacy-peer-deps --prefix client && npm run build --prefix client",
    "start": "nodemon server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.7.2"
  },
  "devDependencies": {
    "bcryptjs": "^2.4.3",
    "concurrently": "^7.5.0",
    "nodemon": "^2.0.20"
  }
}
