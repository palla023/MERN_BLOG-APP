import { makeStyles } from "@mui/styles";
export const useStyles = makeStyles({
  font: {
    fontFamily: "Roboto !important",
  },
});
// In MUI v5, the makeStyles API is no longer recommended because the library has transitioned to an emotion-based styling solution (@mui/system).
// The styled API or the sx prop should be used for modern MUI projects.
